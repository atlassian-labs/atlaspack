---
'@atlaspack/rust': patch
---

Adds additional error detail to various `fs::` function calls throughout the codebase. This is purely as a debugging measure to help us work out where these file not found errors are coming from.

---
'@atlaspack/rust': minor
---

Implement a native CommitRequest to commit asset contents to the database for reading during packaging. Currently does not purge the asset contents from memory, we can make that optimisation later.
